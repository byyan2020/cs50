#include <cs50.h>
#include <stdio.h>

int count_letters(string text)

int main(void)
{
    // getting user input
    string text = get_string("Text:");

    // count letters
    int letters = count_letters(text);

    printf("%i\n", letters);

}

int count_letters(string text)
{
    
}